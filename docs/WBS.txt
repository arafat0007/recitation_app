---

# Work Breakdown Structure (WBS) - Updated

## Project: Mother’s Islamic Audio App

## Platform: Android

## Stack: Kotlin + Jetpack Compose + Firebase Auth + Firestore + Room + DataStore

---

# 1. Project Setup and Authentication Foundation

## 1.1 Create project
* (Previous setup steps...)

## 1.2 Authentication System
### 1.2.1 Configure Firebase Auth
* Enable Email/Password provider in console
* Add `firebase-auth` dependency

### 1.2.2 Auth Data Layer
* Create `AuthRepository` interface
* Implement `AuthRepositoryImpl` with `signIn`, `signUp`, `sendPasswordResetEmail`, and `signOut`

### 1.2.3 Auth UI
* Create `LoginScreen` with:
  * Email/Password fields
  * Password visibility toggle
  * "Forgot Password" link and dialog
  * Bengali error handling
* Create `RegisterScreen` with Name, Email, Password
* Implement `AuthViewModel`

---

# 2. Design System and Navigation

## 2.1 Navigation updates
* Add `Login` and `Register` routes
* Set `Login` as start destination
* Logic to skip Login if user is already authenticated

---

# 5. Owaj / YouTube Feature (Updated)

## 5.1 Owaj Data Layer
### 5.1.1 Update Owaj Model
* Add `isWatched` property (runtime derived)
### 5.1.2 Firestore Watched Status
* Create `user_video_status` collection logic
* Implement `toggleWatchedStatus` and `getWatchedVideoIds`

## 5.2 Owaj UI Flow
### 5.2.1 Selection Screen
* Implement Screen with 2 big buttons: "Watched" / "Not Watched"
### 5.2.2 Paginated List Screen
* Implement Firestore paginated query (20 per page) using cursors (`startAfter`, `endBefore`)
* Add large Next/Previous buttons at bottom
### 5.2.3 Detail Screen Update
* Add "Watched?" Card with large YES/NO buttons
* Connect to `toggleWatchedStatus`

---

# 6. Remote Content Integration
### 6.1 Firestore Indexing
* Create composite index for `owajs` (isPublished, sortOrder)
* Create index for `user_video_status` (userId, isWatched)

---

# Milestones (Updated)

## Milestone 1: Authentication and Foundation Complete
* App starts at Login/Register
* User profile stored in Firestore
* Navigation skeleton ready

## Milestone 2: Owaj MVP with Progress Tracking Complete
* Selection screen (Watched/Not Watched)
* Paginated list (20 items)
* "Watched?" YES/NO toggle working
* YouTube playback

... (Remaining milestones follow)
